# sunshine-bff — 配置入口
# 业务配置唯一来源：Nacos（Data ID: sunshine-bff.yaml）
# 本地维护副本：docs/nacos/sunshine-bff.yaml → scripts/sync_nacos.py 同步线上

spring:
  application:
    name: sunshine-bff
  config:
    import:
      - optional:nacos:sunshine-bff.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        group: SUNSHINE_V2
      config:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
