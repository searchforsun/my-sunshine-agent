# ============================================================
# sunshine-bff — 配置入口
# 业务配置托管在 Nacos（Data ID: sunshine-bff.yaml）
# 本地开发: 使用 application-dev.yaml 覆盖（--spring.profiles.active=dev）
# ============================================================

spring:
  application:
    name: sunshine-bff
  config:
    import:
      - optional:nacos:sunshine-bff.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 8.140.48.6:8848
        username: nacos
        password: nacos
      config:
        server-addr: 8.140.48.6:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
