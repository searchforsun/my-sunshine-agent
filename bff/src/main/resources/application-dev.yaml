# ============================================================
# sunshine-bff — 本地开发配置（--spring.profiles.active=dev）
# ============================================================

server:
  port: 8001

orchestrator:
  base-url: http://localhost:8200

logging:
  level:
    com.sunshine: debug
