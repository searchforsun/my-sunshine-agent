# sunshine-finance — 配置入口
# 业务配置唯一来源：Nacos（Data ID: sunshine-finance.yaml）
# 本地维护副本：docs/nacos/sunshine-finance.yaml → scripts/sync_nacos.py 同步线上

spring:
  application:
    name: sunshine-finance
  config:
    import:
      - optional:nacos:sunshine-finance.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
      config:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
