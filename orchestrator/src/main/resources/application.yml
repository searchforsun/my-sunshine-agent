# sunshine-orchestrator — 配置入口
# 业务配置唯一来源：Nacos（Data ID: sunshine-orchestrator.yaml）
# 本地维护副本：docs/nacos/sunshine-orchestrator.yaml → scripts/sync-nacos.ps1 同步线上

spring:
  application:
    name: sunshine-orchestrator
  config:
    import:
      - optional:nacos:sunshine-orchestrator.yaml
      - optional:nacos:sunshine-workflows.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
      config:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
