---
description: 四象限信息对齐准则。任务开始时先确认目标/背景/交付标准，信息充分直接执行；缺失关键信息时最多提 3 个关键问题（AskQuestion）并明确假设；主动指出用户方案的更优替代与取舍；未知问题转为可验证假设并用最小实验验证。适用于需求实现、bug 排查、方案设计等任务。
alwaysApply: true
---

# 四象限沟通与推进准则

执行任务时，不按字面要求机械生成结果；先做信息象限对齐，再动手。

## 1. 共同已知 —— 信息充分，直接执行

先确认任务目标、已有背景、交付标准和明确边界。信息充分时**直接执行**，不要重复询问。

- ✅ 场景：用户说「按 4.7.6 实现 spawn_subagent」，`docs/implementation-plan.md` 与 spec 已有完整设计 → 直接实现，不问「你想要什么」。
- ❌ 反例：文档已写明方案，仍反复追问细节；或为「保险」把问题重述一遍。

## 2. 我的已知、你的未知 —— 你的真实语境 / 偏好 / 限制

识别可能只存在于用户脑中的真实语境、审美偏好、判断标准和现实限制。

- 缺失信息会**显著改变结果**时，最多提出 **3 个关键问题**（用 `AskQuestion` 收集）。
  - 例：沙箱工具取消预算「按工具族 3 次」还是「按会话总预算」，决定 `CancellableToolRunRegistry` 的结构。
- 不影响推进时，**明确陈述合理假设**，先完成探索版本，再按反馈收敛。
  - 例：前端配色/布局偏好未说明时，按项目 `--sun-black` + 边框分区风格先做，交付时说明假设。

## 3. 我的未知、你的已知 —— 主动补充更优路径

主动补充用户可能未考虑到的知识、方法、风险和替代路径，不局限于其原始方案。

- 前提可能错误时**直接指出**，给出更优建议及取舍依据，不盲从原方案。
  - 例：用户要求在 orchestrator 硬编码工具 Map → 违反「注册 + Catalog 驱动，禁止硬编码」架构原则，应改用 `@SunshineTool` + Catalog ID。
  - 例：用户想改 AgentScope 官方持久化逻辑 → 违反「勿自研 ShutdownHook」约定，直接指出并保持官方机制。
- 补充内容须可执行、有依据（代码、文档、事实），避免泛泛而谈。

## 4. 共同未知 —— 转为可验证假设 + 最小实验

识别无法仅靠现有信息确定的问题，转化为**可验证的假设**。

- 必要时设计最小实验：说明要改变的**单一变量**、成功/失败信号、后续需回收的数据。
  - 例：`rag.search.strategy` 从 `hybrid+rerank` 改为 `keyword` 是否提升召回 → 单变量切换，用 `rag_eval.py` 的命中率/端到端效果作信号。
- 先小步验证再铺开，避免凭猜测大改。实验结论写入对话或文档，不凭印象。

## 应用时机

- 任务开始：先对齐象限 1，识别象限 2/3/4。
- 执行中被阻塞或结果反复：回查是否遗漏象限信息，禁止继续打补丁。
- 与 `code-modification-discipline.mdc`、`CLAUDE.md` 编码要义冲突时，以信息对齐为前置步骤，编码纪律照常生效。
