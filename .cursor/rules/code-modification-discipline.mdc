---
description: 代码修改四项纪律：禁止补丁式修改、代码自解释、无残渣、部署一致性
alwaysApply: true
---

# 代码修改纪律

> 与 `architecture-guardrails.md` 互补；与 `CLAUDE.md`「编码要义」一致。冲突时以本规则为准。

## 1. 禁止补丁式修改

- 禁止用临时 `if` 条件、开关变量（flag/boolean 绕过）、复制粘贴旧代码来“修复”问题。
- 所有修改必须追溯根因并重构：优先从链路建模、SSE/步骤契约、提示词、Catalog 入手。
- 同一 bug 改 2–3 轮仍反复或出新症状，立即停止打补丁，质疑架构与 Catalog 提示词（见 `CLAUDE.md` 编码要义 §1）。
- 模型输出不对：改 Catalog/`/prompts` 或架构，禁止代码截断/摘要/过滤兜底。

## 2. 代码自解释

- 变量/函数名须清晰表达功能：`validateUserInput` 而非 `checkInput`，`buildSseStepDelta` 而非 `handle`。
- 注释仅保留业务规则或决策背景（为什么这么做、约束、权衡），禁止复述代码逻辑。
- Java 方法/变量英文表意；Vue 组件名与 props 语义化；非显然逻辑用中文注释解释**为什么**。

## 3. 无残渣

- 修改后立即清理：死代码、未使用的 import/字段/组件、注释掉的代码块。
- 上新则删旧（同 PR 或紧接 PR）；禁止保留“以防万一”的注释旧实现。
- 重命名/删除符号时全量更新引用，不留悬空导入。

## 4. 部署一致性

- 本地与服务器代码必须一致：交付前通过校验（MD5/构建产物 hash）确认未漂移。
- 后端功能改动后必须重启对应服务的 `start.py`；改 `docs/nacos/*.yaml` 后必跑 `python scripts/sync_nacos.py` 并重启消费服务。
- 前端生产构建须设 `VITE_BFF_STREAM_BASE`；运维统一走 `scripts/*.py`，禁止临时脚本。
