# sunshine-auth — 配置入口
# 业务配置唯一来源：Nacos（Data ID: sunshine-auth.yaml）
# 本地维护副本：docs/nacos/sunshine-auth.yaml → scripts/sync_nacos.py 同步线上
spring:
  application:
    name: sunshine-auth
  config:
    import:
      - optional:nacos:sunshine-auth.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
      config:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
