# sunshine-desensitize — 配置入口
# 业务配置唯一来源：Nacos（Data ID: sunshine-desensitize.yaml）
# 本地维护副本：docs/nacos/sunshine-desensitize.yaml → scripts/sync_nacos.py 同步线上

spring:
  application:
    name: sunshine-desensitize
  config:
    import:
      - optional:nacos:sunshine-desensitize.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        group: SUNSHINE_V2
      config:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
