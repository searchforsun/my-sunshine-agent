# ============================================================
# sunshine-workflow-manager — Nacos 配置
# Data ID: sunshine-workflow-manager.yaml
# Group:   DEFAULT_GROUP
# 表结构 SSOT：docker/mysql/init/13-sunshine-workflow-manager.sql（禁止 Flyway）
# ============================================================

server:
  port: 8230

spring:
  datasource:
    url: jdbc:mysql://ecs4c16g:3306/sunshine_workflow?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false

logging:
  level:
    com.sunshine: debug
