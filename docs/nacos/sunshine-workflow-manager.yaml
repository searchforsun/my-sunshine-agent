# ============================================================
# sunshine-workflow-manager — Nacos 配置
# Data ID: sunshine-workflow-manager.yaml
# Group:   DEFAULT_GROUP
# 首次部署：MySQL CREATE DATABASE sunshine_workflow;
# ============================================================

server:
  port: 8230

spring:
  datasource:
    url: jdbc:mysql://ecs4c16g:3306/sunshine_workflow?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root123
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    com.sunshine: debug
