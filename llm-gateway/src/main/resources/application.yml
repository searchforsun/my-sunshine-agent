# ============================================================
# sunshine-llm-gateway — 配置入口
# 业务配置托管在 Nacos（Data ID: sunshine-llm-gateway.yaml）
# 本地开发: 使用 application-dev.yaml 覆盖（--spring.profiles.active=dev）
# ============================================================

spring:
  application:
    name: sunshine-llm-gateway
  config:
    import:
      - optional:nacos:sunshine-llm-gateway.yaml
  cloud:
    nacos:
      discovery:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
      config:
        server-addr: ecs4c16g:8848
        username: nacos
        password: nacos
        import-check:
          enabled: false
        file-extension: yaml
        group: DEFAULT_GROUP
